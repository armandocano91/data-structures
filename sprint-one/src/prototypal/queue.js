var Queue = function() {
  // Hey! Rewrite in the new style. Your code will wind up looking very similar,
  // but try not not reference your old code in writing the new style.
Object.create(queueMethods);

  var someInstance = {
     storage: {},
      back: 0,
      front: 0,
  };


var queueMethods = {



}
